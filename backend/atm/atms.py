
# this function should add an ATM to the atms table, giving it a unique ID (autoincremented) and pairing it to the user_id passed thru in the function
def addATM(user_id):
    return
# this function should add a transaction to the transactions table
def addTransaction(atm_id):
    return


